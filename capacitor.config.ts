import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'insotec-colaborador-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
